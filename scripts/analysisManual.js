// const path = require("path");
// exports.analysis = function (sourcePath, manualData, scriptData, fileNameCheckAction, htmlCheckAction) {
//
//
// // }
// //
// // function each(configs, callback, preLink, preTitle) {
// //   let link = path.join(preLink, configs.link)
// //   let title = path.join(preTitle, configs.title)
// //   callback(link, title)
// //   if (configs.children && configs.children.length > 0) {
// //     for (let index = 0; index < configs.children; index++) {
// //       each(configs[index], callback, link, title)
// //     }
// //   }
// // }
